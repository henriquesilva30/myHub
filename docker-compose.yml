version: '3.9'
services:
  db:
    image: postgres:18
    restart: always
    environment:
      POSTGRES_USER: myhub
      POSTGRES_PASSWORD: myhub123
      POSTGRES_DB: myhub
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
